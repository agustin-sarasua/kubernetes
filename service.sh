# A service is a load balancer 
# after you create a service, you can scale up and down and the user will not notice it.
# You have to specify the out port and the target port where the app is running in the deployment (pods)
